{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "yarn && yarn start",
            "name": "Run npm start",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}
